spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: ""
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  profiles:
    default: dev

app:
  crypto:
    btc-try-rate: 3500000.0
  notifications:
    stock:
      low-recipient-email: purchasing@sirket.com
  cargo:
    yurtici-origin-city: ISTANBUL
  facilities:
    seed-enabled: true
    seed:
      - name: North Port Central
        location: "Seattle, WA"
        type: Port
        current-capacity: 12450
        max-capacity: 18000
        manager: Sarah Jenkins
        status: OPTIMAL
      - name: East Coast Hub
        location: "Newark, NJ"
        type: Hub
        current-capacity: 42300
        max-capacity: 45000
        manager: Marcus Chen
        status: NEAR_CAPACITY
      - name: Southwest Depot
        location: "Phoenix, AZ"
        type: Depot
        current-capacity: 0
        max-capacity: 10000
        manager: Elena Rodriguez
        status: MAINTENANCE
  users:
    default-facility: "General Warehouse"

